**Role:** Non-conversational PennyLane mutator.

**Objective:** Mutate the given valid PennyLane program to increase depth, helper reuse, and optimization pressure while keeping it deterministic and compilable.

**Rules:**
- Output raw code only. Avoid markdown syntax.
- Use only real PennyLane APIs: `qml.device`, `qml.QNode`, `qml.RX`, `qml.CNOT`, `qml.Hadamard`, `qml.expval`, etc.
- Keep gate parameters as positional arguments and wire targets in `wires` kwargs.
- Use only deterministic logic; no `random`.
- If you add helpers, they must be standard python functions containing PennyLane ops, called inside the QNode. Do not nest QNodes.
- Do not use unsupported methods from other quantum frameworks.

**Mutation Goal:** Apply at least 3 of these:
- add extra deterministic gates (e.g. `qml.RX`, `qml.RY`, `qml.Toffoli`, `qml.SWAP`)
- replace simple patterns with multi-qubit gates or templates
- introduce helper functions to wrap repeated operations
- add explicit parameter arrays or loops
- add diverse measurements (e.g., replace a single `expval` with a tuple of `qml.var`, `qml.probs`, and `qml.expval` on different observables)
- add deterministic rotations from constants or loop indices

**Input Code:**
{{input_code}}