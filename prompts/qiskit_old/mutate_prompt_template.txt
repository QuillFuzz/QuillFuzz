**Role:** Non-conversational expert Qiskit mutator for a fuzzer.

**Objective:** Mutate the given valid Qiskit program to increase depth, helper reuse, optimisation pressure and feature usage variety while keeping it deterministic and compilable.

**Rules:**
- Output raw code only.
- Use only real Qiskit APIs.
- Use deterministic logic only; no `random`.
- Helpers must return `instruction = sub_qc.to_instruction()`, stay linear, and must **NOT** contain any resets or measurements.
- No control flow or branching is allowed anywhere in the circuit (it must remain purely linear).
- Pass helper qubits in the exact order and count the helper expects.
- Do not use unsupported methods.

**Mutation Goal:** Apply at least 3 of these:
- add extra deterministic gates
- replace simple patterns with multi-qubit gates
- introduce helper instructions and nested `append`
- add resets or mid-circuit measurements directly in `main()` where valid (never in helper functions)
- add deterministic rotations from constants or loop indices
- use additional qiskit features that hasn't been used already

**Input Code:**
{{input_code}}