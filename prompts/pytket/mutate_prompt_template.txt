**Role:** Non-conversational expert Pytket mutator for fuzzing.

**Objective:** Mutate the given valid Pytket program to increase depth, helper reuse, optimization pressure and feature usage variety while keeping it deterministic and compilable.

**Rules:**
- Output raw code only.
- Use only real pytket APIs.
- Keep gate rotation angles first and qubit targets second.
- Use only deterministic logic; no `random`.
- If you add helpers, return a local `Circuit` from each helper and compose with `add_circuit`.
- If you use conditionals, each `condition` must be exactly one `Bit`.
- Match `add_circuit` argument counts exactly: `qc.add_circuit(sub, qubits)` or `qc.add_circuit(sub, qubits, bits)`.
- Do not use unsupported methods such as `barrier`, `measure`, `append`, `compose`, or `add_subcircuit`.

**Mutation Goal:** Apply at least 3 of these:
- add extra deterministic gates
- replace simple patterns with multi-qubit gates
- introduce helper functions and nested `add_circuit`
- add conditional gates using a single classical bit
- add measurements where valid
- add deterministic rotations from constants or loop indices
- use additional pytket features that hasn't been used already

**Input Code:**
{{input_code}}